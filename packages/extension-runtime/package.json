{
  "name": "@switchyagain/extension-runtime",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "jsondiffpatch": "^0.7.6",
    "limiter": "^3.0.0",
    "@switchyagain/proxy-engine": "0.0.1"
  },
  "scripts": {
    "build": "npm run clean:dist && npm run build:ts",
    "build:ts": "npm run clean:ts && tsc -p tsconfig.build.json",
    "clean:ts": "node --input-type=module -e \"import {rmSync} from 'node:fs'; rmSync('build-ts', {recursive: true, force: true})\"",
    "clean:dist": "node --input-type=module -e \"import {readdirSync, rmSync} from 'node:fs'; for (const file of readdirSync('.')) if (file === 'index.js' || file.endsWith('.min.js')) rmSync(file, {force: true})\"",
    "typecheck:test": "tsc -p tsconfig.test.json --noEmit",
    "dev": "npm link @switchyagain/proxy-engine && npm link",
    "test": "npm run typecheck:test && TZ=UTC vitest run",
    "typecheck": "tsc -p tsconfig.build.json --noEmit && tsc -p tsconfig.json --noEmit && npm run typecheck:test"
  }
}
